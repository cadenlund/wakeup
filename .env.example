# --- Backend ---
ENV=local                         # local | staging | production
LOG_LEVEL=debug                   # debug | info | warn | error
HTTP_ADDR=:8080
SESSION_DOMAIN=localhost

# --- Database ---
DATABASE_URL=postgres://wakeup:wakeup@localhost:5432/wakeup?sslmode=disable

# --- Redis ---
REDIS_URL=redis://localhost:6379/0

# --- Object storage (MinIO locally, S3 in prod) ---
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=wakeup
S3_FORCE_PATH_STYLE=true          # required for MinIO

# --- Email (Resend) — operator must obtain ---
RESEND_API_KEY=
# Until you verify a domain in Resend, this MUST be `onboarding@resend.dev`
# and sends will only deliver to the email that owns the Resend account.
# Once a domain is verified, set this to `no-reply@<your-domain>`.
RESEND_FROM_EMAIL=onboarding@resend.dev
# The reset email renders TWO buttons — "Open in app" (deep link) and
# "Open in browser" (web URL). The token is appended verbatim to each
# base. In `local`/`test` the backend defaults to:
#   APP = wakeup://reset?token=
#   WEB = http://localhost:8081/reset?token=
# Outside dev BOTH are required (boot fails otherwise) — there is no
# fallback domain because every deployment owns its own.
RESET_PASSWORD_APP_URL_BASE=wakeup://reset?token=
RESET_PASSWORD_WEB_URL_BASE=http://localhost:8081/reset?token=

# --- Voice & video (self-hosted LiveKit, runs in docker-compose) ---
# Defaults match the LIVEKIT_KEYS line in docker-compose.yml — no external account needed.
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecretdevsecretdevsecret
# Discord-style "you've been alone too long" disconnect — when a participant is
# the only person in a room for this long, the lone-kick sweeper drops them via
# LiveKit's RemoveParticipant admin RPC. Default 5m matches Discord; tune lower
# for tighter calls or 0 to disable.
ROOM_LONE_KICK_AFTER=5m

# --- Push notifications (Expo) — operator must obtain after Expo project creation ---
EXPO_ACCESS_TOKEN=

# --- Error tracking (Sentry) — operator must obtain ---
SENTRY_DSN=
SENTRY_ENVIRONMENT=local

# --- CORS ---
CORS_ALLOWED_ORIGINS=http://localhost:8081,exp://localhost:8081

# --- Mobile force-upgrade gate (WAKEUPEXPO.md §4.10) ---
# Minimum mobile-app version the server accepts. Surfaces in /v1/healthz so
# the client's force-upgrade modal can fire when the installed version is
# older. Empty = no minimum. Use semver-style strings.
MIN_CLIENT_VERSION=

# --- Universal links (WAKEUPEXPO.md §10.5) ---
# Populate these to enable /.well-known/apple-app-site-association and
# /.well-known/assetlinks.json. Empty values disable the corresponding
# .well-known file (returns 404).
#
# IOS_APP_ID = "<TEAMID>.<bundle id>", e.g. "ABCD12EFGH.app.wakeup.client"
# ANDROID_PACKAGE = the Play Store package, e.g. "app.wakeup.client"
# ANDROID_SHA256_FINGERPRINTS = comma-joined SHA-256 cert fingerprints
#   (uppercase hex with colons, as `keytool -list -v` produces). Multiple
#   fingerprints support signing-key rotation.
IOS_APP_ID=
ANDROID_PACKAGE=
ANDROID_SHA256_FINGERPRINTS=
