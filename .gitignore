# Go build
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
/bin/
/dist/
/build/
vendor/
go.work
go.work.sum

# Env (NEVER commit real env)
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Coverage
coverage.out
coverage.html
*.coverage

# Build artifacts (bare-name binaries left by `go build ./...`)
/apps/backend/server

# Generated OpenAPI / Swagger spec — regenerated by `just gen-docs` each verify.
# At milestone 13.9 this line is removed and the spec is committed for real.
/docs/openapi/
# Generated swag artifacts inside the backend module — docs.go IS committed
# (cmd/server imports it for /v1/openapi.json), but swagger.json/yaml are
# tooling-only duplicates of /docs/openapi/.
/apps/backend/internal/docs/openapi/swagger.json
/apps/backend/internal/docs/openapi/swagger.yaml

# Logs
*.log

# Local data (docker-compose volumes)
.docker-data/
.minio-data/
.postgres-data/

# Expo / Node (mobile, even though we don't write JS yet)
node_modules/
.expo/
.expo-shared/
dist-mobile/

# Claude Code scheduling lock
.claude/
