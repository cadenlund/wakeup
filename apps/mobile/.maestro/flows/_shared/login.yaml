appId: com.anonymous.mobile
---
# Shared sub-flow used by every authenticated screen-level test.
# Per WAKEUPEXPO §12.7, sub-flows MUST NOT call `clearState` —
# parent flows handle that at their top before runFlow'ing this one.
#
# Real seeded credentials land with backend test fixtures (later
# phase). Until then this stays a no-op assertion so the flow set
# parses cleanly.
- assertTrue: true
