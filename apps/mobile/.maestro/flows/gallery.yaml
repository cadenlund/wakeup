appId: app.wakeup.client
---
# Phase 1.4 gallery — verifies the NativeWind v5 + 15-token x 10
# schemes x 2 modes pipeline lands styled UI on the simulator.
# Deleted when Phase 5.1 ships the real conversations list.
- launchApp:
    clearState: true
- assertVisible: "Theme tokens"
- assertVisible: "Buttons"
- assertVisible: "Badges"
- assertVisible: "Card title"
- assertVisible: "Auth block"
- takeScreenshot: gallery
