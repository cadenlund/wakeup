appId: com.anonymous.mobile
---
# Phase 1.4 gallery — verifies the NativeWind v4 + 15-token × 10
# schemes × 2 modes pipeline lands styled UI on the simulator.
# Deleted when Phase 5.1 ships the real conversations list.
- launchApp:
    clearState: true
- assertVisible: "Theme tokens"
- assertVisible: "Buttons"
- assertVisible: "Badges"
- scrollUntilVisible:
    element:
      text: "Inputs & switches"
- takeScreenshot: gallery
