# Auto-generated by `just gen-client` from the backend's OpenAPI
# spec. openapi-typescript emits canonical output that differs from
# Prettier's format, so re-running gen-client would always unformat
# whatever Prettier rewrote — keeping the file out of Prettier's
# scope avoids the CI churn loop.
lib/api/schema.ts
