# Auto-generated by `just gen-client` (openapi-typescript + orval).
# Generators emit canonical output that differs from Prettier's
# format, so re-running gen-client would always unformat whatever
# Prettier rewrote — keeping these out of Prettier's scope avoids
# the CI churn loop.
lib/api/schema.ts
lib/api/hooks
lib/api/model

# Vendored from sonner — formatted as the upstream package ships it.
sonner.css
